# Plans

Working planning documents that need to survive across sessions and machines (committed to git). Claude saves multi-session plans, migration checklists, and in-progress task state here — not in `/tmp`, the scratchpad, or the home directory.

Session-prep documents do **not** go here — those belong in `campaigns/<name>/sessions/`.

Delete a plan file once the work it tracks is complete.
